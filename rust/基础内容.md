# 基础内容

## rust的数据

变量分为可变量，不可变量。
默认不可变量。

```rust
    let a = 1; // 不可变量
    let mut b = 2;  // 可变量
```

`::` 调用静态方法 
```rust
    let mut guest = String::new();
```

占位符
`{}`
```rust
    println!("a ={},b={}",1,2);
```